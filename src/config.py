# Number of videos to fetch for each keyword
MAX_RESULTS_PER_KEYWORD = 20

# Save progress after every N keywords
CHECKPOINT_INTERVAL = 5

# Delay between API requests (seconds)
REQUEST_DELAY = 1

# Retry failed requests
MAX_RETRIES = 3

# Output dataset
OUTPUT_FILE = "data/raw/youtube_dataset.csv"

# Checkpoint file
CHECKPOINT_FILE = "checkpoints/progress.json"

# Log file
LOG_FILE = "logs/collector.log"